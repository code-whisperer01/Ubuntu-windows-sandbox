# Omega Gaming Sandbox — Python dependencies for omega_sandbox.py
#
# Python (no third-party wheels; stdlib only):
#   pip install -r requirements.txt
#
# System packages (Wine, Vulkan, 32-bit libs) — not installable via pip:
#   ./scripts/install-system-packages.sh
#   See also: requirements-system.txt
#
# Wine prefix runtimes (vcredist, .NET, etc.) after system packages:
#   ./scripts/install-deps.sh
#
# Python version: 3.10+ recommended (3.8+ minimum)
